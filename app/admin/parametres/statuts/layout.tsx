// This file is intentionally empty to use the root layout
// which includes the sidebar and header

export default function StatusLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
